[project]
name = "tp2-smoking-app"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy==2.4.5",
    "scikit-learn==1.9.0",
    "joblib==1.5.3",
]

[tool.vercel]
entrypoint = "api.predict:handler"
